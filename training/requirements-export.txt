# Dependencies for export_dataset.py — run wherever the face-vision
# database and raw snapshot storage are reachable (e.g. on the host next to
# docker-compose, or inside the backend container). Not needed on the GPU
# training server.
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.2.2
ImageHash==4.3.1
Pillow==10.2.0
