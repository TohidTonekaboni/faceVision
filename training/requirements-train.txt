# Dependencies for data_preparation.py, train.py, and train_arcface.py — run
# on the GPU server. No database connectivity required; only the raw
# dataset directory produced by export_dataset.py (copied over separately).
opencv-python-headless==4.10.0.84
albumentations==2.0.8
Pillow==10.2.0
ultralytics==8.4.105
matplotlib==3.10.9
PyYAML==6.0.3
ipykernel==7.3.0

# ArcFace pipeline
torch==2.2.2
torchvision==0.17.2
facenet-pytorch==2.6.0
